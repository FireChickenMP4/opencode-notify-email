{
  "name": "opencode-notify-email",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Email notifications from opencode, via SMTP",
  "scripts": {
    "check": "bun run src/notify.ts --check",
    "send": "bun run src/notify.ts",
    "test": "bun test"
  },
  "dependencies": {
    "nodemailer": "^7.0.9"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.15.13",
    "@types/bun": "^1.4.2",
    "@types/nodemailer": "^7.0.4"
  }
}
